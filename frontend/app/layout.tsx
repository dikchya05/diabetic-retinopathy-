import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DR Detector - AI-Powered Diabetic Retinopathy Analysis",
  description: "Advanced AI-powered tool for detecting and analyzing diabetic retinopathy from retinal fundus images. Get instant analysis with confidence scores and visual explanations.",
  keywords: "diabetic retinopathy, AI, medical imaging, retinal analysis, healthcare",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
